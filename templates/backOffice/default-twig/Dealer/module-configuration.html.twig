<div class="card">
    <div class="card-body">
        <p class="mb-0">
            {{ 'This module has no settings here: dealers are managed from their own section.'|trans({}, 'dealer.bo.default') }}
            <a href="{{ path('dealer') }}">{{ 'Browse dealers'|trans({}, 'dealer.bo.default') }}</a>
        </p>
    </div>
</div>
